import { resourceSchema } from './resource.schema';
import { resourceListSchema } from './resource-list.schema';

export const schemas = [resourceSchema, resourceListSchema];

export default schemas;
